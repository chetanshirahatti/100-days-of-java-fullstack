

class DefaultDemo {

    int number = 50;
}

/* Default access modifier example : Use When you want to allow access to a class, method, or variable only within the same package.
 Data/method should be accessible within the package, package-level operations etc. 
 
 Use when: Internal package usage, Not part of public API
 
 */