

public class ProtectedDemo {

    protected int value = 100;
}

/* Protected access modifier example : Use When you want to allow access to a class, method, or variable within the same package and also to subclasses in other packages.
 Data/method should be accessible to related classes, subclasses etc. 
 
 Use when : Child classes need access, Inheritance is involved */