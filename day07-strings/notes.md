# Day 7 – Strings in Java

## What I learned today:

Today I learned about **Strings in Java**.  
Strings are used to store text data.

---

## 🔹 What is a String?

A String is a sequence of characters.
Strings are commonly used for names, messages, emails, and passwords.

---

## 🔹 Why Strings are important?

Almost every Java program uses Strings.
User input, database data, API data – everything comes as String.

---

## 🔹 String methods

Java provides many built-in methods to work with Strings like:

- length()
- concatenate()
- toUpperCase()
- toLowerCase()
- contains()
- substring()
- replace()

---

## 🔹 String comparison

- `==` checks memory location
- `equals()` checks actual text

Always use `equals()` to compare Strings.

---

## 🧠 Real Life Examples

- Login username and password
- Search box input
- Email validation
- Messages and notifications

---

## ✔ Summary

Today I understood how Strings work in Java.
I learned how to use String methods and how to compare Strings correctly.
